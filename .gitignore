node_modules/
dist/
build/
__pycache__/
*.pyc
venv/
.venv/
.env
*.env.local
.expo/
.angular/
coverage/
*.log
.DS_Store
CLAUDE.md

# Uploaded images live with the server that serves them.
backend/media/

# Local Docker uploads dir (see MEDIA_HOST_DIR in .env)
media-local/

# npm cache, redirected here because the build container has no home dir.
frontend/.npm/
